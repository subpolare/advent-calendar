BOT_TOKEN=your-telegram-bot-token
ADMIN_CHAT_ID=123456789
# (Optional during first run; without it admin commands remain disabled until set)
# Optional: DATABASE_URL=postgresql://postgres:postgres@localhost:5432/users
# Optional: BOT_TIMEZONE=Europe/Moscow
# Optional: STORAGE_DIR=storage
